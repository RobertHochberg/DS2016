package ds2016;

public enum LinkType {
	// no link
	NONE,
	// a link from whatever is being considered
	FROM,
	// a link to whatever is being considered
	TO,
	// there is a link here, no other info
	PRESENT
}
